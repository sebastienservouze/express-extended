export enum ControllerMetadataKeys {
    BASE_PATH = 'base_path', // This is the base path for the controller
    ENDPOINTS = 'endpoints', // This is the list of endpoints for the controller
    MIDDLEWARES = 'middlewares' // This is the list of global middlewares for the controller
}